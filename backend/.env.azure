# Azure Container Apps Environment Variables
# Set these in Azure Portal > Container App > Environment variables

PORT=4000
NODE_ENV=production

# Azure Cosmos DB for MongoDB API connection string
# Get from: Azure Portal > Cosmos DB Account > Connection strings
#MONGO_URI=mongodb://<account-name>:<primary-password>@<account-name>.mongo.cosmos.azure.com:10255/?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000
MONGO_URI=mongodb://semt_app:Aswathykc56111@cluster0.iozn3hx.mongodb.net/semt?retryWrites=true&w=majority

# Generate a secure random string for production
# Example: openssl rand -base64 32
#JWT_SECRET=<replace-with-secure-random-string>
